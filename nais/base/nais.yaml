apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: dp-regel-minsteinntekt
spec:
  image: repo.adeo.no:5443/dp-regel-minsteinntekt:latest
  port: 8093
  liveness:
    path: /isAlive
  readiness:
    path: /isReady
  prometheus:
    enabled: true
    path: /metrics
  replicas:
    min: 2
    max: 4
  resources:
    limits:
      cpu: 1000m
      memory: 512Mi
    requests:
      cpu: 200m
      memory: 256Mi
  vault:
    enabled: true